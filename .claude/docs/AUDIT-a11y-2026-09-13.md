# Auditoría de accesibilidad — WCAG 2.1 AA · 13/09/2026

> Emitida por `accessibility-auditor` sobre el árbol de `mejora-diseno` tras merge de `origin/main`.
> El agente no tiene permiso de escritura; el volcado lo hace el orquestador sin alterar el contenido.

**Conteo** — Bloqueante: 0 · Serio: 1 · Moderado: 5 · Menor: 3
**Veredicto** — cumple AA en lo revisado, con una falla real (4.1.3) y deuda de consistencia.

## Contraste

La tabla firmada en `CLAUDE.md` **sigue cumpliéndose**. Verificado aritméticamente:
`globals.css` usa `--gold-600` en claro (5,40:1 texto/relleno · 3,66:1 borde/superficie) y
`--gold-500` en oscuro (7,94:1 · 7,24:1, por encima del mínimo firmado). No hay regresión.

## Hallazgos

| # | Sev | Criterio | Archivo:línea | Fallo | Arreglo |
|---|---|---|---|---|---|
| 1 | SERIO | 4.1.3 Mensajes de estado | `src/app/comercios/(portal)/_components/buscar-miembro-form.tsx:127-144` | El resultado de la búsqueda aparece sin `aria-live`: un cajero con lector de pantalla no sabe si la membresía es válida sin re-explorar | Envolver el resultado en `aria-live="polite"` con región presente desde el primer render |
| 2 | MODERADO | 4.1.2 | `src/components/ui/sheet.tsx:258-263` | `aria-labelledby` apunta al `div role="document"` interno, no al `<dialog>`; el título puede no anunciarse al abrir en móvil | Mover `aria-labelledby` al elemento `<dialog>` |
| 3 | MODERADO | 2.2.1 Ajuste de tiempo | `src/components/ui/toast.tsx:232-243` | El auto-cierre se pausa con `onMouseEnter` pero no con foco de teclado | Añadir `onFocus`/`onBlur` al mismo manejador de pausa |
| 4 | MODERADO | 4.1.2 | `src/app/admin/miembros/numeros/page.tsx:133-148` | Los filtros «Disponibles/Asignados/Todos» marcan el activo solo por color/variante | Añadir `aria-current="page"` al filtro activo |
| 5 | MODERADO | Regla propia (no falla 2.5.8) | `src/components/ui/button.module.css:83-105` | `Button size="sm"` e `iconOnly sm` miden 36px sin `::after` de `var(--tap-min)` ni `@media (pointer: coarse)`, a diferencia de `Copiar`/`InputButton`. Afecta al menú de acciones de `numeros/page.tsx:171-179` | Replicar el patrón `::after` centrado ya usado en `Copiar` |
| 6 | MODERADO | 1.4.10 Reflow | `src/components/ui/card.module.css:137` | `@media (max-width: 640px)` donde la regla del proyecto exige `@container contenido`; riesgo de reflow incorrecto al 200% de zoom con la barra lateral abierta | Cambiar a `@container contenido (max-width: 640px)` y verificar que hay contenedor ancestro |
| 7 | MENOR | 4.1.3 | `buscar-miembro-form.tsx`, `miembro-form.tsx` | `Alert` de error sin `key={state.error}` (los login sí lo tienen): un mismo mensaje repetido podría no re-anunciarse | Añadir `key` |
| 8 | MENOR | 4.1.2 | `src/components/ui/accion-estado.tsx:70-86` | Alterna estado cambiando solo el texto visible, sin `aria-pressed` | Añadir `aria-pressed` |
| 9 | MENOR | Consistencia | `src/app/comercios/(portal)/_components/resultado-miembro.tsx:23-27` | Usa `Badge` genérico en vez de `StatusBadge` (el texto ya distingue, así que no es fallo WCAG) | Migrar a `StatusBadge` para heredar el patrón punto lleno/hueco |

## Sin hallazgos

`carrusel-destacados.tsx` (sin autoplay, teclado nativo por scroll, `prefers-reduced-motion` correcto) ·
`StatusBadge`/`VenceEn` (forma + texto) · formularios de miembro y de rango (labels, `required`
programático, `aria-describedby` vía `Field`) · `:focus-visible` global · `Modal`/`dialog` nativo ·
`DataList` (semántica de tabla correcta) · `tap-min` en `Copiar` e `InputButton`.

## Requiere prueba manual

1. Lector de pantalla real sobre `Sheet` al abrir en móvil (hallazgo 2).
2. Zoom al 200% con la barra lateral en rail sobre `Card` (hallazgo 6).
3. Navegación completa por teclado de `DropdownMenu`/`popover` en Chrome, Firefox y Safari reales.
